#include "libft.h"
#include <stdio.h>
#include <stdlib.h>

int main()
{
    char i[] = "bu bilgisayarı beyinsizin teki kullanıyor.";
    char m[] = "kopyalanacakstring";

    // ft_memcpy(i, m, 5);
    // printf("%s", i);

    // ft_memmove(m + 6, m, 8);
    // printf("%s" , m);

    // ft_memset(i, 't', 10);
    // printf("%s", i);

    ft_strchr(i, 't');
    
}