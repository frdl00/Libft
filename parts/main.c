#include "libft.h"
#include <stdio.h>

int main ()
{
	// char d[] = "fardal";

	// ft_memset(d, 'Z',8);
	// printf("%s",d);

	int a = 0;

	ft_memset(&a, 255, 4); 
	ft_memset(&a, 214, 1); 

	printf("%d", a);

	//bitlere falan filan hesaplamalarına bak

}